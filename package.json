{
  "name": "cdp-mcp",
  "private": true,
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "inspect": "mcp-inspector",
    "test:e2e": "vitest run --config vitest.e2e.config.ts"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}
